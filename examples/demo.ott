--
-- demo.ott - playing with opetopic type theory
--

let identity : (X : Type) -> X -> X
    	     = \X. \x. x ;

let test : (X : Alg) -> (i : box X (pt (obj X))) -> (x : [ obj X ]>> box i leaf) -> Unit
    	 = \X. \i. tt ; 

tt